name: Send emails daily
on:
  workflow_dispatch:
  schedule:
    - cron: '00 23 * * *'

jobs:
  calculate-and-send:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          repository: ${{ vars.REPOSITORY }}
          ref: ${{ vars.REF }}

      - name: Setup uv
        uses: astral-sh/setup-uv@v7

      - name: Run script
        env:
          ZOTERO_ID: ${{ secrets.ZOTERO_ID }}
          ZOTERO_KEY: ${{ secrets.ZOTERO_KEY }}
          ZOTERO_IGNORE: ${{ vars.ZOTERO_IGNORE }}
          SEND_EMPTY: ${{ vars.SEND_EMPTY }}
          ARXIV_QUERY: ${{ secrets.ARXIV_QUERY }}
          SMTP_SERVER: ${{ secrets.SMTP_SERVER }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SENDER: ${{ secrets.SENDER }}
          RECEIVER: ${{ secrets.RECEIVER }}
          SENDER_PASSWORD: ${{ secrets.SENDER_PASSWORD }}
          MAX_PAPER_NUM: ${{ secrets.MAX_PAPER_NUM }}
          USE_LLM_API: ${{ secrets.USE_LLM_API }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_API_BASE: ${{ secrets.OPENAI_API_BASE }}
          MODEL_NAME: ${{ secrets.MODEL_NAME }}
          LANGUAGE: ${{ vars.LANGUAGE }}
        run: |
          uv run main.py
        # run: |
        #   # 设置最大尝试次数（比如最多试 3 次）
        #   MAX_RETRIES=3
        #   COUNT=0
          
        #   while [ $COUNT -lt $MAX_RETRIES ]; do
        #     echo "==== 开始第 $((COUNT+1)) 次运行 ===="
        #     # 如果命令成功 (&& break)，跳出循环
        #     uv run main.py && break
            
        #     # 运行到这里说明上一条命令失败了，记录失败次数
        #     COUNT=$((COUNT+1))
            
        #     # 如果还没达到最大重试次数，就等待 15 分钟
        #     if [ $COUNT -lt $MAX_RETRIES ]; then
        #       echo "第 $COUNT 次尝试失败，等待 15 分钟后进行重试..."
        #       sleep 15m  
        #     fi
        #   done
          
        #   # 检查如果达到最大次数还是失败了，让整个 Action 报错退出
        #   if[ $COUNT -eq $MAX_RETRIES ]; then
        #     echo "已达到最大重试次数，任务彻底失败！"
        #     exit 1
        #   fi
